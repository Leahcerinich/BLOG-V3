<% include ./partials/header %>

<h1> Index page</h1>

<% blogs.forEach(function(Blog) { %>
    <div>
        <h2>
            <% Blog.title %>
        </h2>
        <img src= <%=blog.image %> >
        <span><% blog.created %></span>
        <p><%= blog.body%> </p>
    </div>

    <% }) %>
    <% include./partials/footer %>